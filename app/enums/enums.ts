export const enum UserType {
  PET_OWNER = "PET_OWNER",
  BICKER = "BICKER",
  JOGGER = "JOGGER",
  NONE = "NONE",
}

export const enum Step {
  NAME = 1,
  EMAIL_AND_PASSWORD,
  GENDER,
  BAND_ID,
  USER_TYPE,
  PET_DETAILS
}
